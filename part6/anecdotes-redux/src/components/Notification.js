import React from 'react';

const Notification = () => {
  const style = {
    border: '1px solid',
    padding: 10,
  };

  return <div style={style}>notification here</div>;
};

export default Notification;
